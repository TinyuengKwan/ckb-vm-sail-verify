# 当前工作树与冻结交付源码的差异复核

2026-09-13。本次核对源码身份、完整差异清单及后续辅助工具的验证来源。
**不是最终 worktree 语义审批，不关闭 `worktree_audit` 或 Week6。**
正式证明通过、文件哈希一致和完整发布候选已获审批是三个不同结论。

**后续变更说明：** 本文对应的快照保留当时身份。
[工作树源码验收器及聚合接线](WORKTREE_SOURCE_VALIDATOR.md)随后改变了源码和审计器清单，
当前工作树不再等于本文报告；下述 449/251/23/178 等数量仅描述本次历史复核。
不回写原报告，也不将新增实现归入旧冻结交付。

## 对比对象

- Git 基点：主仓库 `872d225dc4e16c449be37d92761b20c6aefe853c`，包含当前未提交的工作树覆盖层。
- 冻结交付源码：handoff v1 中 `source-snapshot.json` 文件 SHA-256
  `810b71aa1d4002acce1ad0fc52ef1f37019a6e57550d1c5e381c06b68c5b9999`。
- 本次当前源码：以[复核报告](../../artifacts/boundary-check/worktree-delta-review-UDkMuUOu/report.json)
  和同目录 `current-source-snapshot.json` 为精确身份；它包含本文，不沿用旧快照哈希。
- 正式审计范围：v10 的文件、主证明来源和公开证明来源三个字典的并集，共 178 个路径；
  这是已声明哈希范围，不声称是整个运行时/操作系统依赖闭包。

本次记录预期为主项目 449 文件、相对主 HEAD 的 251 项差异；CKB-VM 489 文件、两项
已采纳补丁差异；Sail-RISC-V 859 文件、无差异。具体机器清单和执行是否成功以报告为准。
不提交、暂存、重置或删除原工作区改动，也不移动原安装/构建目录。

## 冻结交付之后的增量

包含本文的本次快照相对原冻结主项目有 23 项差异：16 份 Markdown、一个交付准备 JSON、
三个辅助入口及其三个测试文件。两个子模块均与原冻结快照相同；23 个变化路径与上述
178 个正式审计路径无交集。原 `step-policy`、公开政策和 v10 的受审来源不变。

这不证明任意未列入 policy 的改动均无影响。本次仅对明确列出的三个新增入口说明范围：

| 新入口及测试 | 实际验证来源 | 作用和限制 |
| --- | --- | --- |
| `restore_fixed_inputs.py` / `test_restore_fixed_inputs.py` | [统一恢复及独立验收](UNIFIED_FIXED_RESTORE.md)，本次重跑 19 项测试 | 只接受固定 handoff；新目录恢复，已有 canonical checkout 拒绝。恢复代码独立于冻结候选；不运行全链或安装宿主 |
| `review_fixed_host_inputs.py` / `test_review_fixed_host_inputs.py` | [静态宿主核验及独立复核](FIXED_HOST_INPUT_REVIEW.md)，本次重跑 18 项测试 | 读取声明文件/ELF 元数据并记录候选库名；不是实际装载或 ABI/完整闭包证明 |
| `stage_fixed_cmake_inputs.py` / `test_stage_fixed_cmake_inputs.py` | [冷配置及 GMP 构建](FIXED_CMAKE_CONFIGURATION.md)，本次重跑 17 项测试 | 固定归档的新目录数据暂存。CMake 执行另有驱动；没有接入正式构建位置或证明政策 |

Markdown 变化是恢复/安装/冷配置记录、状态索引和[核心公开结论修正](PUBLIC_CLAIMS_REVIEW.md)。
handoff JSON 是独立输入准备清单，不是正式准入政策或 release 版本。
逐文件的旧/新字节身份、变更类型、分类和差异文件保存到报告目录，不以扩展名分类代替
语义审批。测试的源码哈希、实际 argv、日志和普通/优化两种模式结果也分别记录。

## 不能从该增量检查推出的结论

1. 原冻结源码本身已有大量相对 HEAD 的改动。仅证明冻结后的 23 项没有触及正式审计
   路径，不等于审批此前全部改动；报告另列全部 HEAD 差异，不能只看新增部分。
2. 原源码快照不包含 Git 忽略的生成物、安装和运行报告。本次没有把它们都装进源码审计，
   其来源及实跑身份仍分别由正式门禁和各自清单负责。
3. 原冻结归档中的旧文档仍保留原字节，不能将本次文档修正归入旧归档或原 CKB commit。
4. 178 路径之外存在实验、资格、发布辅助和文档文件。即使有单独报告，它们也不自动
   由主门禁背书；最终发布须明确哪些文件属于交付、哪些属于历史材料，并逐项审查。
5. 本次不声称版本已冻结、第三方已复现、CI 已覆盖工作树增量或实际发布已完成。

下一步必须在明确的最终交付范围上完成全部来源/生成差异的语义审查，连接对应证据，
再让 release 级检查拒绝缺项和身份漂移。新 Linux 环境及后续发布授权仍需用户提供；
不能用本机清单替代新环境全链和外部实际记录。
