# Recorded outcome of compiling the generated Rust-side Lean model: the
# production CKB-VM execution path, extracted by Charon from the roots in
# crates/proof-extract and translated by Aeneas.
#
# Same two-directional contract as the Sail side: an unexpected failure is a
# regression, and an unexpectedly successful build means this record and the
# documents citing it are stale.
#
# What the compiled definitions contain: `ckb_vm.instructions.execute.execute`
# (the production entry, including the program-counter advance),
# `execute_instruction` (the dispatch), `ckb_vm.instructions.common.add`, and
# `DefaultCoreMachine`'s CoreMachine methods with real bodies. `common::add` is
# present because production reaches it from the root, not because the script
# names it.
#
# Deliberately opaque, each for a stated reason:
#
#   ckb_vm::memory::_   ADD touches no memory, and SparseMemory::load uses
#                       Iterator::skip, which the Aeneas Lean library does not
#                       model.
#   MachineRuntime      The reviewed upstream-plus-patch baseline groups the
#                       dynamic callbacks in this private opaque container.
#                       DefaultMachine is now a structure and the five
#                       register/PC delegates are real translated definitions.
#                       Pause remains an external opaque type. See
#                       extraction/ADOPTION.md; this is not the original commit.
#   Register<u64>::{eq,lt,lt_s,logical_not}
#                       All four are `bool -> u64` via `.into()`; Aeneas emits
#                       `core.convert.FromU64Bool`, which its Lean library does
#                       not define, and the file does not compile with them in.
#                       ADD uses none of them. BEQ does, so a comparison
#                       theorem must resolve this first.
#
# Two external constant axioms were removed by exact --include rules:
#   ckb_vm_definitions.RISCV_GENERAL_REGISTER_NUMBER = 32#usize
#   ckb_vm_definitions.registers.RA = 1#usize
# Both are pure generated defs. theorems/ExtractedConstants.lean checks their
# values and permits only the three observed standard logical axioms.
# Other external functions/types remain in the 51-axiom file and are not all
# explicit --opaque selections. Full dispatch is not a path-sensitive ADD list.
#
# Trust boundary, per VERIFICATION.md section 5: the Aeneas Lean library itself
# has four `sorry`s: Slice.get_unchecked and its spec in Std/Slice.lean, and
# IteratorChars.next/fold in Std/StringIter.lean. Direct ADD reads use
# Slice.index_usize, but a future theorem's helper lemmas and explicit premises
# must also be audited. See ADD_AUDIT.md and audit/*Dependencies.lean.
# This build record does not certify a theorem or approve an axiom allowlist.
status=ok
# 285 defs, 51 axioms, 12 structures (old baseline: 281 defs / 63 axioms).
# These file-level counts are not the final theorem's transitive axiom count.
# About 20s once mathlib is cached; mathlib is the
# expensive part of a cold setup.
target=CkbVmProduction
