# Recorded outcome of compiling the generated Sail Lean model.
#
# `scripts/check_proof_model.sh` builds the model and compares reality against
# this record. It fails in BOTH directions: a new failure is a regression, and
# a build that starts succeeding means this record is stale and the
# documentation that cites it is wrong. Generation succeeding is not evidence
# that the model is usable; this file is where that distinction is recorded.
#
# History, kept because it is the reason the pins are what they are:
#
#   At sail-riscv 27224cc with released Sail 0.20.2 this build FAILED at
#   target 10 of 131 with `unknown namespace LeanRV64D.Defs`.
#   handwritten_support/RiscvExtras.lean opened a `<module>.Defs` namespace
#   that the Sail Lean backend emitted only between 81cb7a52e (2026-06-23) and
#   18105125e (2026-07-20); Sail 0.20.2 (2026-06-22) predates both and emitted
#   no namespace at all. Upstream builds Sail from source for this target
#   (compile-lean.yml uses `sail-version: "latest"`) while pinning 0.20.2 for
#   the C emulator, so it never tested the combination we had pinned.
#
#   Resolved by moving both pins together: sail-riscv to 8f91355e (2026-08-14,
#   "Track upstream Sail change to Lean namespaces") and Sail to a source build
#   at 8eb1fb6b (2026-08-17). Both are now checked by
#   scripts/verify_environment.sh. The materialized configuration hash did not
#   change across the move, so the runtime differential evidence is comparable
#   to what it was before.
status=ok
# 125 .olean files, about ten minutes cold on 24 cores.
target=LeanRV64D
# The generated lakefile requires the Lean support library by BRANCH, so two
# runs can resolve to different code -- observed in one session resolving both
# 79b4d085 and 07946313. check_proof_model.sh pins the lakefile to this
# revision before resolving, and fails if the manifest disagrees.
lean_sail_rev=079463134b9c50450b8393e1566a09fc492a34d9
